/* @oracle/oraclejet-preact: 14.1.0 */
'use strict';

var chai = require('chai');
var translationBundle = require('../../translationBundle.js');

describe('Test translation bundle loader', () => {
    it('loads root bundle', async function () {
        const bundle = await translationBundle["default"](null);
        chai.expect(bundle).not.null;
        chai.expect(Object.keys(bundle).length).greaterThan(0);
    });
    it('loads non-root bundle', async function () {
        const bundle = await translationBundle["default"]('en');
        chai.expect(bundle).not.null;
        chai.expect(Object.keys(bundle).length).greaterThan(0);
    });
});
/*  */
//# sourceMappingURL=translationBundle.spec.js.map
