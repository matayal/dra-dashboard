/* @oracle/oraclejet-preact: 14.1.0 */
'use strict';

var jsxRuntime = require('preact/jsx-runtime');
var preact = require('@testing-library/preact');
var chai = require('chai');
var Button = require('../../Button-07d97191.js');
var hooks_UNSAFE_useTabbableMode = require('../../hooks/UNSAFE_useTabbableMode.js');
require('../../UNSAFE_BaseButton.js');
require('../../BaseButton-9bfe304e.js');
require('../../tslib.es6-6f60dd09.js');
require('../../hooks/UNSAFE_usePress.js');
require('preact/hooks');
require('../../hooks/UNSAFE_useHover.js');
require('../../hooks/UNSAFE_useToggle.js');
require('../../hooks/UNSAFE_useActive.js');
require('../../utils/UNSAFE_classNames.js');
require('../../classNames-55bceee6.js');
require('preact/compat');
require("identity-obj-proxy");
require('../../utils/UNSAFE_interpolations/dimensions.js');
require('../../utils/UNSAFE_arrayUtils.js');
require('../../utils/UNSAFE_size.js');
require('../../_curry1-f1ebd742.js');
require('../../utils/UNSAFE_mergeInterpolations.js');
require('../../_curry3-bc6e6a86.js');
require('../../_curry2-588f7ddc.js');
require('../../_has-2eedb7e0.js');
require('../../utils/PRIVATE_clientHints.js');
require('../../clientHints-e821b5ee.js');
require('../../utils/UNSAFE_mergeProps.js');
require('preact');

afterEach(preact.cleanup);
describe('Test Button component', () => {
    it('render', async function () {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "Press Me" }));
        const label = await content.findByText('Press Me');
        chai.expect(label).not.null;
    });
    it('if both label and title exist, it should show title as tooltip', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { display: "icons", label: "label", title: "title" }));
        const button = content.container.firstElementChild;
        chai.expect(button).to.not.be.null;
        if (button) {
            const tooltipText = button.getAttribute('title');
            chai.expect(tooltipText).to.equal('title');
        }
    });
    it('no default tooltip', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "label" }));
        const button = content.container.firstElementChild;
        chai.expect(button).to.not.be.null;
        if (button) {
            const tooltipText = button.getAttribute('title');
            chai.expect(tooltipText).to.equal(null);
        }
    });
    it('if accessibleLabel exist, it should show as tooltip', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { display: "icons", accessibleLabel: "alabel", label: "button" }));
        const button = content.container.firstElementChild;
        chai.expect(button).to.not.be.null;
        if (button) {
            const tooltipText = button.getAttribute('title');
            chai.expect(tooltipText).to.equal('alabel');
        }
    });
    it('click', async function () {
        let count = 0;
        const increment = () => {
            count++;
        };
        const content = preact.render(jsxRuntime.jsxs("div", { children: [jsxRuntime.jsx(Button.Button, { label: "Press Me", onAction: increment }), jsxRuntime.jsx("span", { children: count })] }));
        const button = await content.findByText('Press Me');
        chai.expect(button).not.null;
        preact.fireEvent.click(button);
        chai.expect(count).to.equal(1);
    });
    it('ENTER', async function () {
        let count = 0;
        const increment = () => {
            count++;
        };
        const content = preact.render(jsxRuntime.jsxs("div", { children: [jsxRuntime.jsx(Button.Button, { label: "Press Me", onAction: increment }), jsxRuntime.jsx("span", { children: count })] }));
        const button = await content.findByText('Press Me');
        chai.expect(button).not.null;
        preact.fireEvent.keyDown(button, { code: 'Enter', key: 'Enter', keyCode: 13, charCode: 13 });
        preact.fireEvent.keyUp(button, { code: 'Enter', key: 'Enter', keyCode: 13, charCode: 13 });
        chai.expect(count).to.equal(1);
    });
    it('Space', async function () {
        let count = 0;
        const increment = () => {
            count++;
        };
        const content = preact.render(jsxRuntime.jsxs("div", { children: [jsxRuntime.jsx(Button.Button, { label: "Press Me", onAction: increment }), jsxRuntime.jsx("span", { children: count })] }));
        const button = await content.findByText('Press Me');
        chai.expect(button).not.null;
        preact.fireEvent.keyDown(button, { code: 'Space', key: 'Space', keyCode: 32, charcode: 32 });
        preact.fireEvent.keyUp(button, { code: 'Space', key: 'Space', keyCode: 32, charcode: 32 });
        chai.expect(count).to.equal(1);
    });
    it('variant borderless', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { variant: "borderless", label: "button" }));
        const button = content.container.querySelector('.oj-c-base-button-borderless');
        chai.expect(button, 'borderless button found').not.null;
    });
    it('variant callToAction', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { variant: "callToAction", label: "button" }));
        const button = content.container.querySelector('.oj-c-base-button-callToAction');
        chai.expect(button, 'callToAction button found').not.null;
    });
    it('variant danger', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { variant: "danger", label: "button" }));
        const button = content.container.querySelector('.oj-c-base-button-danger');
        chai.expect(button, 'danger button found').not.null;
    });
    it('variant outlined', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { variant: "outlined", label: "button" }));
        const button = content.container.querySelector('.oj-c-base-button-outlined');
        chai.expect(button, 'outlined button found').not.null;
    });
    it('variant solid', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { variant: "solid", label: "button" }));
        const button = content.container.querySelector('.oj-c-base-button-solid');
        chai.expect(button, 'solid button found').not.null;
    });
    it('variant ghost', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { variant: "ghost", label: "button" }));
        const button = content.container.querySelector('.oj-c-base-button-ghost');
        chai.expect(button, 'ghost button found').not.null;
    });
    it('tabindex default ', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "button" }));
        const button = content.container.firstElementChild;
        if (button) {
            const tabindex = button.getAttribute('tabindex');
            chai.expect(tabindex).to.equal('0');
        }
    });
    it('size default ', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "button" }));
        const button = content.container.querySelector('.oj-c-base-button-md');
        chai.expect(button, 'default size button found').not.null;
    });
    it('size sm ', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "button", size: "sm" }));
        const button = content.container.querySelector('.oj-c-base-button-sm');
        chai.expect(button, 'sm size button found').not.null;
    });
    it('size md ', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "button", size: "md" }));
        const button = content.container.querySelector('.oj-c-base-button-md');
        chai.expect(button, 'md size button found').not.null;
    });
    it('size lg ', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "button", size: "lg" }));
        const button = content.container.querySelector('.oj-c-base-button-lg');
        chai.expect(button, 'lg size button found').not.null;
    });
    it('disabled default', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "button" }));
        const button = content.container.firstElementChild;
        if (button) {
            const disabled = button.getAttribute('disabled');
            chai.expect(disabled).null;
        }
    });
    it('disabled', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "button", isDisabled: true }));
        const button = content.container.firstElementChild;
        if (button) {
            const disabled = button.getAttribute('disabled');
            const tabindex = button.getAttribute('tabindex');
            chai.expect(tabindex).to.equal('-1');
            chai.expect(disabled).not.null;
        }
    });
    it('display default', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "button", startIcon: jsxRuntime.jsx("span", { class: "startIcon" }), endIcon: jsxRuntime.jsx("span", { class: "endIcon" }) }));
        const startIcon = content.container.querySelector('.startIcon');
        const endIcon = content.container.querySelector('.endIcon');
        chai.expect(startIcon).not.null;
        chai.expect(endIcon).not.null;
    });
    it('display default', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "button", startIcon: jsxRuntime.jsx("span", { class: "startIcon" }), endIcon: jsxRuntime.jsx("span", { class: "endIcon" }) }));
        const label = await content.findByText('button');
        const startIcon = content.container.querySelector('.startIcon');
        const endIcon = content.container.querySelector('.endIcon');
        chai.expect(label).not.null;
        chai.expect(startIcon).not.null;
        chai.expect(endIcon).not.null;
    });
    it('display all', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "button", display: "all", startIcon: jsxRuntime.jsx("span", { class: "startIcon" }), endIcon: jsxRuntime.jsx("span", { class: "endIcon" }) }));
        const label = await content.findByText('button');
        const startIcon = content.container.querySelector('.startIcon');
        const endIcon = content.container.querySelector('.endIcon');
        chai.expect(label).not.null;
        chai.expect(startIcon).not.null;
        chai.expect(endIcon).not.null;
    });
    it('display icons', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "button", display: "icons", startIcon: jsxRuntime.jsx("span", { class: "startIcon" }), endIcon: jsxRuntime.jsx("span", { class: "endIcon" }) }));
        const label = content.findByText('button');
        const startIcon = content.container.querySelector('.startIcon');
        const endIcon = content.container.querySelector('.endIcon');
        chai.expect(Object.keys(label).length === 0).to.be.true;
        chai.expect(startIcon).not.null;
        chai.expect(endIcon).not.null;
    });
    it('display text', async () => {
        const content = preact.render(jsxRuntime.jsx(Button.Button, { label: "button", display: "label", startIcon: jsxRuntime.jsx("span", { class: "startIcon" }), endIcon: jsxRuntime.jsx("span", { class: "endIcon" }) }));
        const label = await content.findByText('button');
        const startIcon = content.container.querySelector('.startIcon');
        const endIcon = content.container.querySelector('.endIcon');
        chai.expect(label).not.null;
        chai.expect(startIcon).null;
        chai.expect(endIcon).null;
    });
    it('with TabbableModeContext isTabbable=true', async () => {
        const content = preact.render(jsxRuntime.jsx(hooks_UNSAFE_useTabbableMode.TabbableModeContext.Provider, Object.assign({ value: { isTabbable: true } }, { children: jsxRuntime.jsx(Button.Button, { label: "button" }) })));
        const button = await content.findByText('button');
        const tabindex = button.getAttribute('tabindex');
        chai.expect(tabindex).to.equal('0');
    });
    it('with TabbableModeContext isTabbable=false', async () => {
        const content = preact.render(jsxRuntime.jsx(hooks_UNSAFE_useTabbableMode.TabbableModeContext.Provider, Object.assign({ value: { isTabbable: false } }, { children: jsxRuntime.jsx(Button.Button, { label: "button" }) })));
        const button = await content.findByText('button');
        const tabindex = button.getAttribute('tabindex');
        chai.expect(tabindex).to.equal('-1');
    });
});
/*  */
//# sourceMappingURL=Button.spec.js.map
