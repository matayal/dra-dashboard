/* @oracle/oraclejet-preact: 14.1.0 */
'use strict';

var jsxRuntime = require('preact/jsx-runtime');
var preact = require('@testing-library/preact');
var chai = require('chai');
var Collection = require('../../Collection-2647e7ee.js');
require('preact');

describe('Test Collection component', () => {
    it('render', async function () {
        const data = [
            { id: 'e1', name: 'Employee 1' },
            { id: 'e2', name: 'Employee 2' }
        ];
        const content = preact.render(jsxRuntime.jsx("ul", { children: jsxRuntime.jsx(Collection.Collection, Object.assign({ items: data }, { children: (context) => {
                    return jsxRuntime.jsx("li", { children: context.data.name });
                } })) }));
        const emps = await content.queryAllByText('Employee', { exact: false });
        chai.expect(emps.length).eq(2);
    });
});
/*  */
//# sourceMappingURL=Collection.spec.js.map
