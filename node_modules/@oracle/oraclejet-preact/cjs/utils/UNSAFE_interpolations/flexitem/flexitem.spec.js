/* @oracle/oraclejet-preact: 14.1.0 */
'use strict';

var flexitem = require('../../../flexitem-0c1e9a72.js');
var chai = require('chai');
require("identity-obj-proxy");
require('../../../keys-326d80d5.js');
require('../../../_curry1-f1ebd742.js');
require('../../../_has-2eedb7e0.js');

const { alignSelf } = flexitem.flexitemInterpolations;
// TODO: Is this test actually useful?  What kind of testing should be done on interpolations?
describe('flexitem alignSelf', () => {
    const table = [
        [{}, {}],
        [{ foo: 'bar' }, {}]
        // [{ alignSelf: 'end' }, css`${{ alignSelf: "end", }}`],
    ];
    table.forEach((testParams) => {
        const [input, expected] = testParams;
        it(`when ${input} returns ${expected}`, async function () {
            const actual = alignSelf(input);
            chai.expect(actual).to.deep.equal(expected);
        });
    });
});
/*  */
//# sourceMappingURL=flexitem.spec.js.map
