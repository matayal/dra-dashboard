/* @oracle/oraclejet-preact: 14.1.0 */
'use strict';

var clientHints = require('../../../clientHints-e821b5ee.js');
var chai = require('chai');

describe('clientHints', () => {
    it('checks consistency', () => {
        // Get hints from userAgent string
        const hintsFromString = clientHints.getClientHints(navigator.userAgent);
        // Get default hints (depending upon browser support,
        // may be computed from NavigatorUAData)
        const hintsFromDefault = clientHints.getClientHints();
        // check that clientHints attributes, as determined by either NavigatorUAData processing
        // or userAgent string parsing, are consistent
        chai.expect(hintsFromString.browser).to.equal(hintsFromDefault.browser);
        chai.expect(hintsFromString.browserMajorVersion).to.equal(hintsFromDefault.browserMajorVersion);
        chai.expect(hintsFromString.deviceType).to.equal(hintsFromDefault.deviceType);
        chai.expect(hintsFromString.platform).to.equal(hintsFromDefault.platform);
    });
});
/*  */
//# sourceMappingURL=clientHints.spec.js.map
