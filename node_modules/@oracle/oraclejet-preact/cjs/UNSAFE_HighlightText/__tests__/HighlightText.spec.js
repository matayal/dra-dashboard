/* @oracle/oraclejet-preact: 14.1.0 */
'use strict';

var jsxRuntime = require('preact/jsx-runtime');
var preact = require('@testing-library/preact');
var chai = require('chai');
var HighlightText = require('../../HighlightText-5bdc12cb.js');
require('preact');
require("identity-obj-proxy");

afterEach(preact.cleanup);
describe('Test HighlightText component:', () => {
    it('render no highlighting', async function () {
        const content = preact.render(jsxRuntime.jsx(HighlightText.HighlightText, Object.assign({ matchText: "x" }, { children: "testing" })));
        const spans = content.container.querySelectorAll('span');
        chai.expect(spans.length, 'unhighlighted text').eq(0);
    });
    it('render highlighting', async function () {
        const content = preact.render(jsxRuntime.jsx(HighlightText.HighlightText, Object.assign({ matchText: "t" }, { children: "testing" })));
        const spans = content.container.querySelectorAll('span');
        chai.expect(spans.length, 'highlighted text').eq(2);
    });
});
/*  */
//# sourceMappingURL=HighlightText.spec.js.map
