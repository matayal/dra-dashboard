import { ListProps } from './List.types';
/**
 * Props for the ListView Component
 */
export declare type Props<K, D> = Pick<ListProps<K, D>, 'accessibleSummary' | 'children' | 'currentKey' | 'currentItemVariant' | 'data' | 'gridlines' | 'onCurrentKeyChange' | 'onItemAction' | 'onLoadRange' | 'onSelectionChange' | 'selectedKeys' | 'selectionMode' | 'viewportConfig'>;
/**
 * Component that renders items as a flat list.
 * This component is nom-virtualized and renders all rows provided in the data prop.
 */
export declare function ListView<K extends string | number, D>(props: Props<K, D>): import("preact").JSX.Element;
