import { ComponentProps } from 'preact';
import { Icon } from './Icon';
import { useTheme } from '../hooks/UNSAFE_useTheme';
declare type Props = Omit<ComponentProps<typeof Icon>, 'viewBox'>;
declare type ThemeType = ReturnType<typeof useTheme>;
declare type ThemeIcons = Record<ThemeType['name'], typeof Icon>;
export declare const withThemeIcon: (themeIcons: ThemeIcons) => (props: Props) => import("preact").JSX.Element;
export {};
