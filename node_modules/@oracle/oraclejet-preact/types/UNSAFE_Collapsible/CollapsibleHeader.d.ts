import { HTMLAttributesSignalExcluded } from '../utils/UNSAFE_attributeUtils';
declare type IntrinsicProps = Pick<HTMLAttributesSignalExcluded<HTMLDivElement>, 'id' | 'children'>;
declare type HeaderProps = IntrinsicProps & {
    id: string;
    contentId: string;
    isDisabled: boolean;
    isExpanded: boolean;
    iconPosition?: 'start' | 'end';
    variant?: 'basic' | 'horizontal-rule';
    toggleHandler: (target: EventTarget | null) => void;
};
/**
 * Header subcomponent
 */
export declare const CollapsibleHeader: ({ children, id, contentId, isDisabled, isExpanded, iconPosition, variant, toggleHandler }: HeaderProps) => import("preact").JSX.Element;
export {};
